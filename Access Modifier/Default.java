//import animals.Default_Dog;           // ❌ ERROR — can't even import!
// Dog is not public

// Main class for default access example
public class Default
{
    public static void main(String[] args)
    {
        //Default_Dog d = new Default_Dog();    // ❌ ERROR — not accessible outside package
    }
}
